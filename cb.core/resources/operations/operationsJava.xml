<?xml version="1.0" encoding="UTF-8"?>
<operations>
	<category name="Basic" expanded="true">
		<operation text="End"
			toolTip="Method end point. Additing any operation after `End` will cause an error"
			image="images/operations/icons/begin_end.gif">
			<node type="return">
				<keywords>
					<return_value default=""
						wizzard_toolTip="Function's return value" />
				</keywords>
				<template>
					return ${return_value};
				</template>
			</node>
		</operation>
		<operation text="Value initialization"
			toolTip="Input values section"
			image="images/operations/icons/function.gif">
			<node type="function">
				<keywords>
					<value1 default="value1"
						wizzard_toolTip="Value wich will be initialized" />
					<value2 default="value2"
						wizzard_toolTip="Value wich will initialize value1" />
				</keywords>
				<template>
					${value1} = ${value2};
				</template>
			</node>
		</operation>
	</category>
	<category name="Conditions" expanded="true">
		<operation text="If" toolTip="Input values section"
			image="images/operations/icons/condition.gif" />
	</category>
	<category name="Input/Output" expanded="true">

		<operation text="Output" toolTip="Output values section"
			image="images/operations/icons/input_output.gif" />
		<operation text="Value initialization"
			toolTip="Input values section"
			image="images/operations/icons/function.gif">
			<node type="function">
				<keywords>
					<value default="string" wizzard_toolTip="Output value" />
				</keywords>
				<template>
					System.out.println(${value});
				</template>
			</node>
		</operation>
	</category>
	<category name="Min/Max" expanded="true">
		<operation text="minString" toolTip="Find minimal string"
			image="images/operations/icons/function.gif">
			<node type="function">
				<keywords>
					<collection default="collection" wizzard_toolTip="Source String-type collection" />
					<collection default="destination" wizzard_toolTip="Value where min String will be stored" />
				</keywords>
				<template>

				</template>
			</node>
		</operation>
		<operation text="maxString" toolTip="Find maximal string"
			image="images/operations/icons/function.gif" />
						<node type="function">
				<keywords>
					<collection default="collection" wizzard_toolTip="Source String-type collection" />
					<collection default="destination" wizzard_toolTip="Value where min String will be stored" />
				</keywords>
				<template>
					
					if(!${collection}.isEmpty()) {
						${destination} = ${collection}.get(0);
						for(String string: ${collection}) {
							if(string.length() > ${destination}.length()) {
								${destination} = string;
							}
						}
					} else {
						${destination} = null;
					}
				</template>
			</node>
		<operation text="minNumber" toolTip="Find minimal number"
			image="images/operations/icons/function.gif" />
	</category>
	<category name="Sorting" expanded="true">
		<operation text="BubbleSort"
			toolTip="Use bubble sort algorithm"
			image="images/operations/icons/function.gif" />
		<operation text="QuickSort"
			toolTip="Use quick sort algorithm"
			image="images/operations/icons/function.gif" />
	</category>

</operations>